# Files that must never be uploaded as public assets.
# Serving rules live in worker.js; this list keeps source and tooling out of
# the asset store entirely.
server.js
package.json
worker.js
wrangler.jsonc
.assetsignore
.wrangler/
.dev.vars*
.env*
api/
